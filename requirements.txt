# Simplified dependency pin for a modern Python (3.11-3.12) environment.
#
# This lists the *direct* dependencies with minimum compatible versions. For a
# fully-resolved, hash-pinned lockfile, install pip-tools and run:
#   pip-compile requirements.in
#
# Notes:
#   - tensorflow >=2.17 is required for NumPy 2.x and Python 3.12 support.
#   - tensorflow-datasets supplies real-image datasets (FER-2013, when available).
#   - pillow is used to resize FER-2013 (48x48) down to the 28x28 pipeline size.
#   - scikit-learn supplies the Olivetti Faces dataset, used as a facial-
#     recognition dataset since FER-2013 was removed from tensorflow-datasets'
#     catalog and now requires a manually supplied fer2013.csv (see README).
#   - On Windows, use Python 3.13: TensorFlow has no cp314 Windows wheel yet
#     (stable or nightly). See README.md "A note on Python 3.14".

matplotlib>=3.8
notebook>=7.0
numpy>=2.0
pillow>=10.0
scikit-learn>=1.4
tensorflow>=2.17
tensorflow-datasets>=4.9
